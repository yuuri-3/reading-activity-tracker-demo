export type IconProps = {
  className?: string;
  /** Icon size in px (number) or any CSS length (string) */
  size?: number | string;
  /** CSS color value (e.g. "var(--foreground)", "currentColor") */
  color?: string;
};
