# TK-033 書籍詳細の書籍メモタグ表示と絞り込み

このチケットは、書籍詳細画面の「書籍メモ」に、紐づく記録のタグを表示し、タグで絞り込めるようにする。あわせて、今回の調査で判明した TK-011 移行漏れ（停止画面の原因）を、必要な UID のみに限定して解消する。

## ゴール

- 書籍詳細画面の書籍メモカードに、紐づく記録タグが表示される
- 書籍詳細画面の「書籍メモ」セグメントで、タグ絞り込みができる
- 既存データ（`record.bookMemoId` 紐づけ済み）にも適用される
- 停止画面の原因となる TK-011 の残件 UID が解消される

## 対象

- [src/app/pages/BookSingleView.tsx](../../src/app/pages/BookSingleView.tsx)
- [src/app/components/ListCard.tsx](../../src/app/components/ListCard.tsx)
- [functions/src/admin/backfillTk011.ts](../../../functions/src/admin/backfillTk011.ts)

## 実装状況

Status: ✅ 完了

## 受け入れ条件

達成チェック:

- [x] 書籍詳細画面の書籍メモカードにタグが表示される
- [x] 書籍詳細画面の書籍メモセグメントでタグ絞り込みが動作する
- [x] `bookMemoId` 紐づけ済みの既存データでもタグ表示/絞り込みができる
- [x] `npm run -s typecheck` が通る
- [x] 停止画面の直接原因（TK-011 残件）が 0 になっている

## 作業内容

### 1) 書籍メモカードにタグ表示を追加

- `ListCard` の `BookNote` バリアントに `tags` / `tagsNode` を追加
- 書籍メモ本文の下にタグ表示行を追加

### 2) 書籍詳細画面で書籍メモタグを逆引きし、絞り込みを追加

- `BookSingleView` で `records` から `bookMemoId -> tagIds[]` を構築
- セグメントが「書籍メモ」のときだけ、タグフィルタ UI（`すべて` + タグ）を表示
- 選択タグに一致する書籍メモのみ表示する

### 3) 停止画面の原因 UID のみ TK-011 を再実行

- 調査結果（`project=yomzoy`, `db=(default)`）
  - TK-011 残件: `ijvETlwsPiYuNDLA8HBsY1NwRfv1` のみ（records 日時 5件分 = 15 issues）
- 実行（write）:
  - `npm --prefix functions run build && node functions/lib/admin/backfillTk011.js --project=yomzoy --uid=ijvETlwsPiYuNDLA8HBsY1NwRfv1 --write --allowed-uids=ijvETlwsPiYuNDLA8HBsY1NwRfv1 --out=./.tmp/tk011-ijv-write.json`
- 事後確認（dry-run）:
  - `node functions/lib/admin/backfillTk011.js --project=yomzoy --uid=ijvETlwsPiYuNDLA8HBsY1NwRfv1 --out=./.tmp/tk011-ijv-post-dry-run.json`
- 結果:
  - write: `recordsUpdated: 5`, `failureCount: 0`
  - post dry-run: `recordsUpdated: 0`, `failureCount: 0`

## 非ゴール

- TK-026 cleanup（`books.memos[]` の一括削除）
- 書籍メモに対するタグ直接編集の新データモデル導入

## 注意

- 停止画面の条件は TK-011 の型不整合であり、TK-026 未実施のみでは停止画面条件にならない
- 今回は方針に従い、停止画面を引き起こす UID のみ最小範囲で是正した
