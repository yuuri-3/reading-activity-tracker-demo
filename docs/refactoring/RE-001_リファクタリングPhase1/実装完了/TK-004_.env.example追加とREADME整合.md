# P0-TK-004 `.env.example` 追加と README の整合

このチケットは、README が `.env.example` を参照しているのに実ファイルが無く、セットアップが詰まる状態を解消します。

## ゴール

- 新規環境で迷わず起動できる（必要な環境変数が明示される）
- README の手順が最新の実装と一致する

## 対象

- `.env.example`（新規追加）
- `README.md`（手順の修正）
- Firebase / Functions / CI で使う env 一覧

## 実装状況

Status: ✅ 完了

## 受け入れ条件

達成チェック:

- [x] `.env.example` が存在し、必須/任意のキーが明記されている
- [x] README のセットアップ手順に矛盾がない（ローカルは `.env.local` 前提）
- [x] 秘匿すべき値（サーバ側シークレット等）が `.env.example` に入っていない

## 作業内容

### 1) 必要な env を棚卸し

- フロント必須（例: `VITE_FIREBASE_*`）
- 機能フラグ（例: `VITE_ENABLE_BACKEND_GUEST_MERGE`）

#### 棚卸し結果（現状コード/CI からの暫定）

主な参照元:

- `src/app/firebase/firebase.ts`
- `vite.config.ts`
- `.github/workflows/deploy-firebase-hosting.yml`
- `src/vite-env.d.ts`（型定義）

| key                                 | 必須 | 用途                                              | 設定場所                                         | 備考                                                                                  |
| ----------------------------------- | ---- | ------------------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------- |
| `VITE_FIREBASE_API_KEY`             | 必須 | Firebase Web SDK 設定                             | ローカル `.env.local` / GitHub Actions Variables | フロントに埋め込まれる値（シークレットではないが、コミットはしない）                  |
| `VITE_FIREBASE_AUTH_DOMAIN`         | 必須 | Firebase Web SDK 設定                             | ローカル `.env.local` / GitHub Actions Variables | 例: `xxx.firebaseapp.com`                                                             |
| `VITE_FIREBASE_PROJECT_ID`          | 必須 | Firebase Web SDK 設定 / CI deploy の project 指定 | ローカル `.env.local` / GitHub Actions Variables | workflow では `FIREBASE_PROJECT_ID` にも流用                                          |
| `VITE_FIREBASE_APP_ID`              | 必須 | Firebase Web SDK 設定                             | ローカル `.env.local` / GitHub Actions Variables |                                                                                       |
| `VITE_FIREBASE_STORAGE_BUCKET`      | 任意 | Firebase Web SDK 設定                             | ローカル `.env.local` / GitHub Actions Variables | 現状は未設定でも起動は可能だが、Storage を使う場合は必要                              |
| `VITE_FIREBASE_MESSAGING_SENDER_ID` | 任意 | Firebase Web SDK 設定                             | ローカル `.env.local` / GitHub Actions Variables |                                                                                       |
| `VITE_FIREBASE_MEASUREMENT_ID`      | 任意 | Analytics（使う場合のみ）                         | ローカル `.env.local` / GitHub Actions Variables | 未設定の場合は無効化される想定                                                        |
| `VITE_ENABLE_BACKEND_GUEST_MERGE`   | 任意 | ゲスト統合のバックエンド経路を有効化              | ローカル `.env.local` / GitHub Actions Variables | `"true"` のときのみ有効。CI（本番ビルド）は workflow 側で未設定時に既定 `true` を注入 |
| `VITE_ENABLE_OCR_HANDWRITTEN_MEMO`  | 任意 | OCR（手書きメモ）導線の表示                       | ローカル `.env.local` / GitHub Actions Variables | `"true"` のときのみ有効。既定は OFF（未設定）                                         |
| `VITE_BASE`                         | 任意 | Vite の `base`（配信パス）                        | ローカル `.env.local` / CI                       | 未設定時は `/`。サブパス配信時に必要                                                  |

※ `.env.example` には入れない（シークレット）:

- `FIREBASE_SERVICE_ACCOUNT`（GitHub Actions Secrets）: Firebase デプロイ用サービスアカウント JSON

### 2) `.env.example` 作成

- 何をどこで設定すべきか（ローカル / GitHub Actions Variables / Firebase 側）も一緒に記載

### 3) README 更新

- `.env.example` を前提にした手順へ修正

## 非ゴール

- 環境分離（staging/prod）の導入

## 注意

- `.env` はコミットしない（`.gitignore` 前提）

## 決定事項

- ローカル手順は `.env.local` を使用する（`.env.example` をコピーして作成）

## 関連課題（本チケット対象外）

- `VITE_ENABLE_BACKEND_GUEST_MERGE` の dev/prod 既定値を揃えるか
  - 現状: ローカル未設定=OFF / CI 本番ビルド未設定=ON（workflow により既定 true）
